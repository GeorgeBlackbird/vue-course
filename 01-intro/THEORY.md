# Тема 01: 🚀 Введение во Vue.js: Приложение Vue и основы синтаксиса шаблонов

Привет! 👋 В предыдущей теме мы подготовили всё необходимое для работы. Теперь пришло время познакомиться с самим Vue.js поближе. Эта тема заложит фундамент твоего понимания этого замечательного фреймворка.

## 🤔 Что такое Vue.js?

**Vue.js** (произносится /vjuː/, как английское слово "view") — это прогрессивный **JavaScript-фреймворк** для создания **пользовательских интерфейсов (UI)**. Что это значит на практике?

* **JavaScript-фреймворк:** Vue предоставляет набор инструментов и правил для структурирования твоего JavaScript-кода, упрощая разработку интерактивных веб-страниц.
* **Пользовательские интерфейсы:** Vue фокусируется на той части веб-приложения, которую видит и с которой взаимодействует пользователь. Это кнопки, формы, списки, анимации и всё остальное, что делает сайт "живым".
* **Прогрессивный:** Ты можешь использовать Vue для небольших улучшений на существующей странице или для построения сложного одностраничного приложения (SPA) с нуля. Его можно внедрять постепенно.

Vue славится своей **простотой в изучении**, **гибкостью** и **высокой производительностью**.

> Vue был создан Эваном Ю (Evan You), бывшим сотрудником Google, который хотел взять лучшие идеи из других фреймворков (таких как Angular и React) и создать что-то более легковесное и доступное.

## 🌟 Ключевые концепции Vue.js

У Vue есть несколько фундаментальных идей, которые делают его таким мощным и удобным:

### 1. Декларативный рендеринг (Declarative Rendering)

Вместо того чтобы вручную писать JavaScript-код для поиска HTML-элементов и изменения их содержимого (императивный подход), во Vue ты **декларативно** описываешь, как твой интерфейс должен выглядеть в зависимости от данных.

**Пример идеи:**
Ты говоришь Vue: "Хочу, чтобы здесь отображалось имя пользователя". И когда имя пользователя меняется, Vue сам обновляет это место на странице. Тебе не нужно писать код типа `document.getElementById('username').textContent = newName;`.

Vue использует знакомый тебе HTML в качестве шаблонов и "связывает" его с данными твоего JavaScript-кода.

### 2. Реактивность (Reactivity) ⚡

Это одна из "магических" особенностей Vue. Система реактивности означает, что когда твои **данные** (JavaScript-объекты) изменяются, **DOM** (то, что отображается на странице) **автоматически обновляется**, чтобы отразить эти изменения.

Представь, что у тебя есть переменная `count` со значением `0`. Ты выводишь её на страницу. Когда ты увеличиваешь `count` до `1`, Vue автоматически перерисовывает ту часть страницы, где отображается `count`, чтобы показать новое значение.

Эта система позволяет тебе думать о состоянии приложения, а Vue берет на себя заботу об синхронизации этого состояния с отображением.

### 3. Компонентный подход (Component-Based Architecture) 🧱

Vue позволяет строить пользовательский интерфейс из **компонентов**. Компонент — это небольшой, независимый и переиспользуемый кусочек кода, который инкапсулирует в себе HTML-разметку, JavaScript-логику и CSS-стили.

**Аналогия:** Представь конструктор LEGO. У тебя есть разные типы кирпичиков (кнопка, поле ввода, карточка товара). Ты можешь использовать эти кирпичики много раз в разных частях своей модели (веб-страницы). Каждый кирпичик имеет свою логику и внешний вид.

Такой подход делает код более организованным, масштабируемым и легким для понимания и поддержки. Мы будем много работать с компонентами в следующих темах!

## 🚀 Приложение Vue: Точка входа

Каждое Vue-приложение начинается с создания **экземпляра приложения Vue** с помощью функции `createApp`. Этот экземпляр является "корнем" твоего Vue-приложения.

Если ты заглянешь в файл `src/main.js` в проекте, который мы создали с помощью Vue CLI, ты увидишь что-то похожее:

```javascript
// src/main.js
import { createApp } from 'vue'
import App from './App.vue' // Наш корневой компонент

// Создаем экземпляр приложения, передавая ему корневой компонент App
const app = createApp(App)

// "Монтируем" приложение в HTML-элемент с id="app"
// Этот элемент находится в public/index.html
app.mount('#app')
```

Давай разберем ключевые моменты `createApp`:

* `createApp(RootComponent)`: Эта функция принимает **корневой компонент** (в нашем случае это `App.vue`) в качестве аргумента. Корневой компонент — это самый верхний компонент в дереве компонентов твоего приложения.
* `.mount('#app')`: Этот метод "монтирует" (т.е. вставляет и запускает) наше Vue-приложение в определенный HTML-элемент на странице. Обычно это `div` с `id="app"`, который находится в файле `public/index.html`.

Внутри объекта, передаваемого в `createApp` (или чаще, внутри самого корневого компонента), мы определяем данные и методы, которые будут управлять нашим приложением. Например, так можно определить данные:

```js
// Пример простой конфигурации приложения
import { createApp } from 'vue'

const app = createApp({
  data() {
    return {
      message: 'Привет от Vue!', // Наши данные
      count: 0
    }
  }
})

app.mount('#app')
```

* `data()`: Это функция, которая **возвращает объект**. Свойство этого объекта становятся реактивными данными, доступными в шаблоне.

**Vue 2 vs Vue 3:** Если ты встретишь старые туториалы, там может использоваться `new Vue({})` для создания экземпляра. В `Vue 3` (который мы используем) правильный способ — это `createApp()`.

## 📜 Основы синтаксиса шаблонов

Vue использует **HTML-подобный синтаксис шаблонов,** который позволяет декларативно связывать отрисованный DOM с данными базового экземпляра приложения. Весь Vue-шаблон является валидными HTML, который могут распарсить все браузеры и HTML-парсеры.

Давай рассмотрим два основных элемента синтаксиса шаблонов:

### 1. Интерполяция текста (Text Interpolation) `{{ }}`

Самый простой способ связать данные с DOM — это интерполяция текста с использованием синтаксиса "двойных фигурных скобок" (Mustache syntax, "усы", 콧수염):

```html
<div id="app">
  <p>{{ message }}</p>
  <p>Счетчик: {{ count }}</p>
</div>
```

Если в `data()` нашего Vue-приложения есть свойства `message` и `count`:

```js
// ... в конфигурации createApp
data() {
  return {
    message: 'Всем привет!',
    count: 10
  }
}
// ...
```

То на странице отобразится:

```html
<p>Всем привет!</p>
<p>Счетчик: 10</p>
```

Содержимое внутри `{{ }}` — это JavaScript-выражение. Vue будет вычислять его и подставлять результат. Важно, что эти данные **реактивны**: если `message` или `count` изменятся в JavaScript, текст на странице обновится автоматически!

*(Здесь был бы кстати простой GIF, показывающий, как изменение значения переменной в data() приводит к обновлению текста на странице)*

### 2. Директивы (Directives)

Директивы — это специальные атрибуты с префиксом `v-`. Их задача — применять побочные эффекты к DOM, когда значение выражения изменяется.

Представь их как инструкции для Vue, что нужно сделать с этим HTML-элементом.

**Общий вид директивы:** `v-имя-директивы="выражение"`

Например, директива `v-if` условно отображает элемент:

```html
<div id="app">
  <p v-if="isVisible">Сейчас меня видно!</p>
  <button @click="isVisible = !isVisible">Переключить видимость</button>
  </div>
```

```js
// ... в конфигурации createApp
data() {
  return {
    isVisible: true
  }
}
// ...
```

В этом примере, если `isVisible` равно `true`, параграф будет отображен. Если `false` — он будет удален из DOM.

Существует множество встроенных директив (`v-bind`, `v-for`, `v-on`, `v-model` и др.), и мы подробно изучим самые важные из них в следующих темах. Они — ключевой инструмент для создания динамических интерфейсов.

## ✨ Итог

В этой теме мы сделали первый шаг в мир Vue.js:
* Узнали, что Vue — это прогрессивный JavaScript-фреймворк для создания UI.
* Познакомились с его ключевыми концепциями: декларативным рендерингом, реактивностью и компонентным подходом.
* Поняли, что каждое Vue-приложение начинается с `createApp()` и монтируется в DOM.
* Рассмотрели основы синтаксиса шаблонов: интерполяцию `{{ }}` для вывода данных и познакомились с понятием директив `v-`.

Это только начало, но уже сейчас ты должен иметь представление о том, как Vue "думает" и работает.

## 🔗 Полезные ссылки
* **Официальная документация Vue.js (Введение):** https://vuejs.org/guide/introduction.html
* **Официальная документация Vue.js (Основы синтаксиса шаблонов):** https://vuejs.org/guide/essentials/template-syntax.html
* **Приложение Vue API (`createApp`):** https://vuejs.org/api/application.html

В следующей части (`PRACTICE.md`) мы применим эти знания на практике!